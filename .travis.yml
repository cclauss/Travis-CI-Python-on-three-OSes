language: python            # this works for Linux but not for macOS or Windows
matrix:
  include:
    - name: "Python 3.7.1 on Xenial Linux"
      python: 3.7           # this works for Linux but not for macOS or Windows
      dist: xenial          # required for Python >= 3.7
    - name: "Python 3.7.3 on macOS"
      os: osx
      osx_image: xcode10.2  # Python 3.7.3 running on macOS 10.14.3
      language: shell       # 'language: python' is an error on Travis CI macOS
      # python: 3.7         # 'python:' is ignored on Travis CI macOS
    - name: "Python 3.7.3 on Windows"
      os: windows           # Windows 10.0.17134 N/A Build 17134
      language: shell       # 'language: python' is an error on Travis CI Windows
      # python: 3.7         # 'python:' is ignored on Travis CI Windows
      before_install:
        - choco install python  # this install takes about 1 min 30 sec
      env:
        - PATH=/c/Python37:/c/Python37/Scripts:$PATH
        - python3=py

install:
  - py --version || true
  - py3 --version || true
  - py37 --version || true
  - py3.7 --version || true
  - pip3 install --upgrade pip
# 'python' points to Python 2.7 on macOS but points to  Python 2.7 on Linux and Windows
# 'python3' is a 'command not found' error on Windows
script: python3 platform-info.py # || py platform-info.py
notifications:
  email: false
